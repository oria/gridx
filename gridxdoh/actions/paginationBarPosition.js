// define([
	// 'dojo/_base/array',
	// 'dojo/_base/query',
	// 'dojo/dom-class',
	// '../GTest'
// ], function(array, query, domClass, GTest){
	// GTest.actionCheckers.push(
	// {
		// id: 'paginationBarPosition 1',
		// name: 'set pagination bar to a new position, call refresh() to take effect',
		// condition: function(grid){
			// return grid.paginationBar && grid.paginationBar.arg('position');
		// },
		// action: function(grid, doh, done){
			// grid.paginationBar.position = 'bottom';
			// grid.body.refresh();
// 			
			// doh.t(query('.gridxPaginationBar', grid.footerNode).length, 
				// 'paginationbar locate in the footer');
// 
			// grid.paginationBar.position = 'top';
			// grid.body.refresh();
// 			
			// doh.t(query('.gridxPaginationBar', grid.headerNode).length, 
				// 'paginationbar locate in the header');
// 			
// 				
			// grid.paginationBar.position = 'both';
			// grid.body.refresh();
// 			
			// doh.t(query('.gridxPaginationBar', grid.footerNode).length, 
				// 'paginationbar locate in the footer');
// 
			// doh.t(query('.gridxPaginationBar', grid.headerNode).length, 
				// 'paginationbar locate in the header');
// 			
			// done.callback();
		// }
	// }
// );
// });
