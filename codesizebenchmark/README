Compress gridx code by closure compiler, and generate a report on gridx code size.

How to use:
1. Put this package together with gridx package.
2. Run closureAll.bat
3. A directory gridx-build will be generated, containing all compressed files
4. A report.html will be generated.

Arguments:
-r: Generate report only, do not compile any files.

Requirements:
1. JAVA 1.6
2. NodeJS
